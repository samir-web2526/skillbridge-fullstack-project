model Category {
  id          String         @id @default(uuid())
  name        String         @unique
  description String?
  tutor       TutorProfile[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name])
  @@map("category")
}
